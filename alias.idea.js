/**
 * 本文件对构建无任何影响，仅作为 WebStorm 识别用。
 * 进入 WebStorm preferences -> Language & Framework -> JavaScript -> Webpack，选择这个文件即可。
 */
module.exports = {
	resolve: {
		alias: {
			"@": __dirname,
		},
	},
};
